# teste_node
Estudo NodeJS - criando uma lista de produtos

O node_modules não está versionado.
Para instalar as dependências descritas no package.json é preciso rodar o comando:
npm run build

Para subir o servidor, rode o comando:
npm start

Para acessar a aplicação:
http://localhost:3000/
